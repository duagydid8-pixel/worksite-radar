# worksite-radar 프로젝트 메모

## 세션 종료 규칙
매 세션이 끝나기 전에 아래 "세션 기록" 섹션에 오늘 작업한 내용을 간략히 추가할 것.
형식: `- YYYY-MM-DD: <한 줄 요약>`

---

## 세션 기록

- 2026-05-19: 전자카드 대조 페이지 기능 대폭 확장 — 팀별 미타각 제외 체크박스(localStorage 저장), XERP 미등록 타각자 표시, 타업체 착오태그 감지(업체명 파싱), 이름불일치 타각 자동 감지(생년월일 폴백 매칭), 미타각 모바일 공유 이미지(팀별 배지, 이름+생년월일 앞자리)
- 2026-08-26: 연차관리 페이지(AnnualLeaveManagementPage) 복잡도 개선 — "직원별 연차 현황" 표에서 이름 클릭 시 선택되는 직원과 "연차 사용 입력" 폼의 직원 상태가 분리되어 있던 것을 하나로 통합(이제 표에서 클릭하면 입력폼도 같은 사람으로 자동 전환 + 선택 행 강조), 표의 "구분"(재직구분) 열과 입력폼의 "구분"(휴가종류)이 같은 단어라 헷갈리던 것을 "재직구분"으로 분리 명명
- 2026-08-26: 연차 명단 업로드 양식에 "보상휴가"(유급 보상휴가) 열 추가됨 — 파서가 이 열을 읽어 직원별 startingCompLeave로 저장하고, 사용내역 입력 시 보상휴가 잔여분을 연차보다 먼저 차감하도록 deriveLeaveStatusRows 로직 수정. 표/요약카드에 보상휴가 잔여 열 추가. 실제 업로드 파일(연차 엑셀 업로드.xlsx, 24명)로 파싱 검증 완료
- 2026-08-26: 발생연차를 더 이상 엑셀의 "발생연차"/"잔여연차" 열 값을 신뢰하지 않고 항상 입사일 기준(calculateAccruedLeave)으로 앱이 재계산하도록 변경 — 담당자마다 엑셀 산정 방식이 달라 실제와 어긋나는 사례(조용선: 엑셀 7 vs 입사일 계산 9) 발견 후 수정. startingAccrued/startingRemaining/startingBasisDate 필드 및 관련 로직(calculateMonthsAfter) 제거해 로직 단순화, startingUsed(과거 실사용)와 startingCompLeave(보상휴가)만 엑셀에서 가져옴. 잔여연차는 항상 발생-사용으로 파생 계산
- 2026-08-26: calculateAccruedLeave를 달력월 버킷 방식(입사 당월 즉시 1일, 그 뒤 달 바뀔 때마다 +1)에서 "입사일과 같은 날짜(day)에 도달해야 그 달치 1일이 발생"하는 정확한 만개월 계산(근로기준법 60조 2항 방식)으로 수정. 예: 입사일 3/15면 4/15에 1일, 4/14까지는 0일(기존엔 3/31에 이미 1일, 4/1에 2일로 과다 계산됐었음)
